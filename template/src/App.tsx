import styled from "styled-components";

const StyledAppContainer = styled.div`
  padding: 2rem;
`;

export default function App() {
  return (
    <StyledAppContainer>
      <h1>Hello, World!</h1>
    </StyledAppContainer>
  );
}
